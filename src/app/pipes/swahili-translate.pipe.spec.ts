import { SwahiliTranslatePipe } from './swahili-translate.pipe';

describe('SwahiliTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new SwahiliTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
